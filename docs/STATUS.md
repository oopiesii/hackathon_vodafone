# Поточний статус і передавання роботи

## Налаштування вигляду дашборда; права на файли блокують злиття (2026-09-20, 01:00 UTC)

- Підстава: пряме доручення користувача продовжити нічний цикл і закривати наявні прогалини, плюс окреме прохання дати змогу згортати та переставляти блоки дашборда.
- Зроблено: кожен блок «Сьогодні» має в заголовку згортання, а в режимі «Налаштувати вигляд» — переміщення вище/нижче, лічильник позиції та «Скинути». Порядок і згорнуті блоки зберігаються в `localStorage` браузера; склад блоків і права лишаються за кодом. Новий модуль `apps/web/src/lib/dashboard-layout.ts`, `Card` отримав необов'язковий проп `widget`.
- Перевірено: typecheck трьох пакетів і збірка; 12 Node-тестів (5 нових на узгодження порядку) PASS; Chromium 1440/390 у світлій і темній темах через підставний API — згортання, переміщення, збереження після перезавантаження, «Скинути», без горизонтального переповнення. Знімки розглянуто очима. Production не змінювався, БД не чіпали, реальних Telegram/LLM викликів немає.
- **Не злито в `/opt/ufv`.** Ця сесія працює під користувачем `deploy`, а частина каталогів (`apps/web/src/components/dashboard`, `charts`, `services/analyst`) і 58 каталогів у `.git/objects` належать `root` після попередньої сесії. Ні правка на місці, ні коміт із worktree неможливі.
- Де лежить робота: гілка `feat/dashboard-widgets`, коміт `e6edc78` у клоні `/home/deploy/ufv-dev` (від `4578d3f`); обидва коміти як серія патчів — `/home/deploy/ufv-dashboard-widgets.patch`. Знімки перевірки — `/home/deploy/ufv-dev/artifacts/widgets-check/shots/`, поза Git.
- Як внести від root: `git -C /opt/ufv fetch /home/deploy/ufv-dev feat/dashboard-widgets && git -C /opt/ufv merge --ff-only FETCH_HEAD` або `git -C /opt/ufv am /home/deploy/ufv-dashboard-widgets.patch`. Щоб такі блокування не повторювались, варто повернути власність робочого дерева: `chown -R deploy:deploy /opt/ufv`.
- Далі в тому самому колі закрито **WP7 (Б)**: статус дій команди тепер видно у стрічці значком, а в деталях матеріалу є розділ «Статус дій» — адміністратор змінює вибором зі списку, viewer бачить прочерк. У «Що зараз» додано перехід «Змінити у стрічці». Права не чіпали: редагує лише `collector:manage`, як перевіряє наявний `tests/test_actions.py`. Групового запису для сигналу немає навмисно — сигнал це кілька матеріалів, а API зберігає статус окремо для кожного. Коміт `a767333`, перевірка — `artifacts/widgets-check/feed-check.mjs`, 1440/390 у двох темах.
- Відомий наслідок: довільний порядок блоків різної ширини може лишати порожнє місце в ряду 12-колонкової сітки. Типовий порядок порожнин не має; «Скинути» повертає його.
- Реліз і відкат без змін: production лишається на `4bb17ff` (`night-20260919T224055Z`), а коміти `c0364f9`–`4578d3f` (зокрема локальний рантайм) також ще не розгорнуті — потрібні кроки від root із [LOCAL_RUNTIME](LOCAL_RUNTIME.md).

## Документаційна передача іншому інструменту (20.09.2026, Київ)

За прямим проханням користувача оновлено [PLAN_NIGHT](PLAN_NIGHT.md): додано фактичні статуси I1–I7 і WP0–WP10, останній завершений пакет **I6**, частково реалізовані WP5/WP7 та наступну фазу 2. У [MORNING_HANDOFF](MORNING_HANDOFF.md) додано порядок продовження, стан Git/worktree, артефакти й команди перевірок; у [NIGHT_RUN](NIGHT_RUN.md) — явну позначку паузи. Код, БД й production цим оновленням не змінювалися; нові тести не запускалися. Попередні перевірки й реліз — у записі нижче. Розробка залишається на паузі.

## Поточний пакет завершено; пауза за дорученням користувача (2026-09-19 22:48 UTC)

- Підстава: **«finish current task and take a pause»**. Завершено поточні виправлення I6, перевірки й реліз; нова фаза 2/I7 не починається. Увесь нічний план не оголошується виконаним: два чисті кола залишаються незавершеними.
- **Розгорнуто `4bb17ff`**: API/UI та analyst — `night-20260919T224055Z`; manifest `/var/lib/ufv/releases/night-20260919T224055Z.json` **verified о 22:41:58 UTC**. Pipeline/collectors залишаються на `night-20260919T221752Z`; повторного restart collectors і нових міграцій немає.
- Виправлено всі п'ять груп нижче: запити dashboard, денні стовпчики тижня з точними переходами, формулювання й видимий scope шаблонного зведення, операторські лічильники, українська діагностика Operations. Новий LLM-ключ не додавався; Telegram `llm_allowed` — **0 джерел**.
- Код: **136 backend-тестів PASS**; після злиття — 6 S2/query-тестів і curated acceptance PASS. Build/typecheck, 2 Node-тести групування тижня та 9 unit-тестів producer PASS. Production fixtures не створювалися; `ufv_checks` вільна, тимчасовий API зупинений.
- Після останнього релізу: public routes/health/сусідній сервіс PASS; live API — **0/33/34** прийнятих за 24h/7d/30d, Vodafone 7d **2**, усі **8** денних стовпчиків узгоджені зі стрічкою. Час API **1,071 / 4,760 / 0,413с** відповідно; це одиничні вимірювання, не SLA. Analyst має свіжий heartbeat і `waiting_key`.
- Chromium на 1440/390, дві теми: **4/4** live-сценарії «денний стовпчик → feed → доказ → назад» та **16/16** комбінацій Sources/RSS/inbox/Operations без JS/HTTP5xx/layout-помилок. Вручну розглянуто всі 8 PNG графіка/зведення й 16 основних знімків цих сторінок. Приватні артефакти: `artifacts/night-i6-live`, `artifacts/night-i6-postdeploy`; реальні тексти не додавалися в Git.
- Залишковий борг: SQL операторського підрахунку 7d близько 3,8 с; повільнішу альтернативу відхилено. Агенти завершили роботу; запущені production-сервіси продовжують працювати.
- Точний відкат останнього пакета: `deploy/night-release.sh --rollback /var/lib/ufv/releases/night-20260919T224055Z.json` — повертає **API й analyst** до попереднього повного релізу, не змінює collectors/processor чи адитивну схему. Відкат не виконувався.
- Передача: [MORNING_HANDOFF](MORNING_HANDOFF.md), [TEAM_BRIEF](TEAM_BRIEF.md), [I6 review](NIGHT_I6_REVIEW.md). Нижче — історія етапів; попередні формулювання «триває» не є активними задачами під час паузи.

## I6: огляд production після релізу — виправлення (20.09, 01:35 Київ)

- Механічний Chromium-прогін: **80/80** комбінацій сторінки/розміру/теми без переповнень, помилок JavaScript і HTTP 5xx. Окремо відкрито й розглянуто **156 PNG**: 88 головного потоку та 68 незалежного огляду Telegram/RSS/Users, включно з верхом/серединою/низом довгих сторінок. Це не перегляд кожного рядка довгих списків. Знімки з реальними текстами лишаються приватними в `artifacts/night-full-release-20260919`, не в Git.
- Незалежний production-аудит: runtime-ролі п'яти сервісів, дозволені/заборонені grants, checksum0030/0031, RSS8дозволених/5заблокованих, Telegram68джерел без LLM, живий `waiting_key` — PASS. Перший зріз coverage вже був complete: 81джерело, 0dirty. SQL/API збігаються:7д33/30д34, NEG13584 із35654 (38,0995%).
- Один реальний ручний refresh: Telegram підхопив за **1,883с**, RSS — за **2,107с**. RSS завершив8джерел о22:21:45UTC; Telegram оновив41джерело й о22:23:25UTC завершив запит як `deferred`: решта чекають лімітів/черги, очікування не скорочувалося. Синтетики у production немає.
- Підтверджені дефекти, за пріоритетом виправлення:
  1. 7d dashboard відповідає близько10с: operator count класифікує весь вхід до часового фільтра, growth повторює semantic lookup для кожного snapshot. Оптимізація A в окремому worktree, без міграції.
  2. Тижневий графік має168тонких погодинних стовпчиків і повторені підписи годин без дат. Групуємо за київськими днями зі збереженням точних меж вибраного вікна в посиланні на докази.
  3. Sources і шаблонний S2 помилково обіцяють правила або називають effective curated результати «прийнятими правилами». Scope дозволених AI-джерел захований у згортці. Уточнюємо producer, legacy-відображення й видимий підпис покриття.
  4. Операторські RSS/inbox називають попередній відбір результатом фінальної стрічки; числа вкладок inbox без розрядів. Уточнюємо підписи, не підмінюючи первинні лічильники semantic-даними.
  5. Telegram→Стан показує англомовні діагностичні значення (`enabled`, `database catch-up active`, назви audit-подій). Переклад відомих значень, технічні деталі — у згортці.
- Пряме нове доручення користувача: **«finish current task and take a pause»**. Завершуємо цей пакет виправлень, цільові перевірки та API+analyst реліз; потім пауза без початку фази2/I7. Collectors повторно не перезапускати. Два чисті кола всього застосунку ще не виконані.

## Повний нічний реліз — розгорнуто (19.09, 22:19 UTC / 20.09, 01:19 Київ)

- **API/UI, processor, Telegram/RSS collectors і analyst:** images `ufv-api:night-20260919T221752Z`, `ufv-pipeline:night-20260919T221752Z`, `ufv-analyst:night-20260919T221752Z`; git `6ba09d9`. Manifest `/var/lib/ufv/releases/night-20260919T221752Z.json` — `verified` о22:19:12 UTC. Єдиний дозволений нічний restart collectors **використано**; надалі їх функціонально не перезапускати.
- Видимі WP2/WP3/WP4/WP8a/S2: «Оновити зараз» і стани черги, демо мережі, сценарний калькулятор, Threads/X slots, Sources зі станом analyst. **Analyst healthy, `waiting_key`**, реальний ключ не додавався. Telegram `llm_allowed=0`; разовий Gemini-відбір збережено.
- **0012, 0014–0016, 0030, 0031 застосовані до production — не редагувати.** Місячний підсумок читає фізичні покоління, приховує неповні дані й показує час зрізу. Legacy RSS scope підтриманий вузько з перевіркою прав; старі дозволені результати не загублено.
- Перед релізом: 133 backend tests, 5 bridge tests, typecheck/build, повторний curated acceptance з ролями/спільним доступом/dirty місяцем/доказами та Chromium320–1440 — PASS. Після: health, свіжі worker heartbeats, login/feed/inbox/Chromium, анонімний401 і сусідній сервіс — PASS. Caddy/PostgreSQL/NATS не перезапускалися.
- Live API приймання: **24h0 / 7d33 / 30d34**, Vodafone за7днів **2**. Числа джерел, конкурентів і Vodafone збігаються зі стрічками за їхніми посиланнями; доказ відкривається. Контрольний dashboard під паралельним browser load: **3,779с / 10,333с / 0,249с** відповідно; це не SLA. Повільний7d профілюється як кандидат наступного доопрацювання.
- Відкат: `deploy/night-release.sh --rollback /var/lib/ufv/releases/night-20260919T221752Z.json`. Повертає API bridge22:15, попередні processor/collectors і прибирає вперше доданий analyst. Адитивна схема лишається; bridge не видає dirty-місяць за повний. Старі manifests для цього відкату не використовувати.
- **Активно:** повний огляд80 production-екранів1440/390 у двох темах, незалежна звірка runtime прав/агрегатів, перевірка ручного refresh. I6 і наступні кола полірування ще не завершено; [ранкова передача](MORNING_HANDOFF.md) поки чернетка.


## Bridge API — розгорнуто перед повним нічним релізом (19.09, 22:16 UTC)

- API image `ufv-api:night-20260919T221508Z`, git `fc44580`, manifest `/var/lib/ufv/releases/night-20260919T221508Z.json` має `verified`. Додано атомарну перевірку повноти місяця для сумісного відкату після 0030; до 0014 збережено чинний запит. Неповний місяць після міграції дає зрозумілий 503, без часткових успішних чисел. Гонку появи схеми між запитами перевірено. [Контракт відкату](ROLLBACK_BRIDGE_REVIEW.md).
- 5 цільових тестів, build/typecheck, production health/login/dashboard/feed/inbox/Chromium та сусідній сервіс — PASS. Окремо live30d: **34** згадки, 200 за **4,422 с**. Це контрольний час одного запиту, не SLA. Collectors/processor не змінено.
- Повний нічний пакет зведено в main `46753de`: 133 backend-тести, повторний curated API/browser acceptance після 0031 і build/typecheck пройдено. Створено protected schema backup перед застосуванням нових міграцій. Починається реліз API + processor + два collectors + analyst; єдиний нічний restart collectors ще не використаний до фактичного switch.
- Після повного релізу використовувати **його новий manifest**: він повертає цей bridge image. Старі manifests 21:53/22:15 повертають API без належного coverage й після 0030 не є безпечним вибором відкату.


## Нічна інтеграція 0030 — активна перевірка (20.09, 01:02 Київ)

- `feat/night-ui` містить перевірений API/UI-реліз доступу, curated-контракт і `0030_curated_atomic_rollups.sql`. Паралельний GitHub-коміт `e359c46` з історичним прототипом збережено звичайним merge; main `4ff95b9` опубліковано без force push. Зміни прототипу не перенесено в робочий React.
- Перерахунок 50 джерел перевірено read-only на реальному обсязі: сума SELECT **9,174 с**, найбільше джерело **3,267 с**, 0 перевищень 4 с. Це вимір SELECT без записів, не SLA. Processor оновлює джерела окремими транзакціями з часовими межами та backoff; NATS-consumer продовжує роботу. Деталі й межі — [CURATED_INTEGRATION_CANDIDATE](CURATED_INTEGRATION_CANDIDATE.md).
- **18 цільових тестів пройдено**; новий/повторний pending-вхід і повторна обробка після появи батьківського допису не приховують готовий accepted-зріз. Зміна тексту, людського рішення, модельної мітки, контексту або прав одразу інвалідує його. Час покоління показує можливу затримку операторських лічильників.
- Триває повний backend-прогін злитого дерева лише на `ufv_checks`, незалежний огляд фінальної SQL/worker-зміни та acceptance сценарію dashboard → джерела → докази. Production **ще не отримував 0030/analyst/нічні слоти**, collectors не перезапускалися. Telegram `llm_allowed` не вмикався.


## Нічний API/UI-реліз доступу — розгорнуто (19.09, 21:53 UTC)

- **Образ `ufv-api:night-20260919T215217Z`, git `98c4a8c`.** Ізоляція клієнтського кешу за сеансом і правами запобігає показу попередніх admin-даних після входу viewer у тій самій вкладці. Shared feed і докази прив'язані до перевіреного `scope_id`: пізня cookie іншого посилання не розширює поточний доступ. Подробиці — [AUTH_CACHE_REVIEW](AUTH_CACHE_REVIEW.md).
- Змінено **тільки API/UI**. Collectors, processor, NATS і Caddy збережено без перезапуску; єдиний нічний restart collectors ще не використано. Нових міграцій цей реліз не додає; production лишається на 0011/0013 та 0020–0022 разом із попередньою схемою.
- До розгортання: typecheck/build; **11 браузерних сценаріїв**, **2 shared API tests** на `ufv_checks`, **8 симуляцій release/rollback** — PASS. Після: health, анонімний feed401, вхід, dashboard, feed, inbox, showcase у Chromium, сусідній `h1hs.com/api/health` — PASS; manifest `verified` 21:53:15 UTC. Контейнери необраних сервісів звірено за image/running/start time.
- Для вкладок зі старим JavaScript потрібне перезавантаження. Старий shared-клієнт без прив'язки scope отримує409 і має повторно відкрити своє посилання; cookie сам по собі більше не дає доступу до feed/documents без scope.
- Відкат однією командою: `deploy/night-release.sh --rollback /var/lib/ufv/releases/night-20260919T215217Z.json`. Попередній образ — curated-реліз. Маніфест і backup image збережено поза Git.
- Наступне: нічні WP2/WP3/WP4/WP8a/S2 уже зведено з curated у `feat/night-ui`; 0030 ще оптимізується, production її не отримував. Повне нічне доручення та кола доопрацювання **не завершено**.

## Нічне узгодження перед наступним релізом (20.09, 00:48 Київ)

- Паралельний curated-реліз зафіксовано в main `581d0d7` та push. Його global semantic gate, окремі NEG/IRONIC/SAD, Vodafone за 7 днів і ступені уваги збережено в нічній інтеграції. Нові або застарілі Telegram-матеріали не повертаються до старого словникового відбору.
- Підготовлено WP2 (refresh + видимий offline/stale), WP3 (демо мережі й соціальні слоти), WP4 (калькулятор), WP8a/S2 (сервіс, права, runtime без ключа, перевірені цитати). Це ще не production-реліз нічної інтеграції.
- I6 знайшов і виправив кеш попереднього користувача, гонку shared-cookie, старі числа під новим фільтром, застарілий зелений AI, хибне місячне «збігів немає», приховану поточну вкладку, сирі стани/масиви меж доступу. Подробиці: [NIGHT_I6_REVIEW](NIGHT_I6_REVIEW.md), [AUTH_CACHE_REVIEW](AUTH_CACHE_REVIEW.md).
- Chromium: 6 curated-виглядів (1440/768/390 × дві теми), stale refresh/AI, докази/назад, калькулятор, мережа й операторські вкладки. Знімки переглянуто як зображення. Одне закриття браузера на останньому мобільному сценарії не відтворилося в точковому повторі; причина не встановлена. Раніше 80 read-only production-перевірок пройшли; повний візуальний огляд і фінальні кола ще не закриті.
- Підготовлена `0030` узгоджує фізичні агрегати, semantic invalidation і S1 continuous. 13 цільових SQL-сценаріїв пройшли в rollback-транзакціях; незалежний огляд не знайшов High/Critical. Профіль великого джерела перевищив 4 с, тому SQL оптимізується перед застосуванням. Production 0030 не отримував.
- Окремий API/UI-реліз виправлень доступу готується без restart collectors. Єдиний нічний restart collectors ще **не використаний**. Telegram `llm_allowed` не вмикався.


## Семантичний відбір головного екрана — розгорнуто (19.09, 21:34 UTC)

- Пряме доручення користувача: прибрати політичний шум із головної статистики/стрічки, підключити виконаний Gemini-прогін, показати Vodafone та пояснити реакції; ступінчастий червоний акцент для високих значень.
- Виправлено розрив: `/analysis` читав модель, а головні `/` і `/feed` — старі словникові правила. Тепер тематичні показники та переходи читають актуальну модельну класифікацію; нові/застарілі pending, review та відсіяне відокремлені. Ручні рішення для поточної версії мають пріоритет; viewer/shares зберігають scope.
- **Міграції 0020–0022 застосовані**: окремі curated views, без переписування raw/mentions і старих міграцій. Нічні worktree 0012/0014–0016 не застосовувалися цим інкрементом до production. У спільній ufv_checks вони вже були від інших агентів. Повний контракт: [CURATED_DASHBOARD.md](CURATED_DASHBOARD.md).
- Перевірений зріз після deploy: **24h — 0 прийнятих; 7d — 33; 30d — 34**. Це час публікації, не час збору. Vodafone — **2** (17 і 18 вересня), тепер явна кнопка за 7 днів. Нові надходження ще не проходять безперервний AI: cutoff разового Telegram-прогону 20:07:46 UTC показаний у UI. Нулі не є доведеним відсутнім негативом.
- Негативні emoji відокремлені від іронії й суму. Пороги акценту 25/50/75% за ≥20 реакцій на ≥3 матеріалах; для високих сигналів — 1–2/3–4/≥5. Позначені як припущення демо. Є текстові рівні та іконка, без миготіння. Показані реальні дати збережених лічильників і watch-policy: типово 5 хв / 1 год / 6 год, з можливими затримками.
- Перевірки: typecheck/build; 14 граничних значень attention; власний workflow у `ufv_checks` без TRUNCATE чужих даних — політичний шум, старе rules-rejected, часові вікна, місячні показники, stale/deleted контекст, ручні рішення, viewer/shares, Chromium 320/390/768/1440 у двох темах. Production — три періоди, точна відповідність метрик/джерел стрічці, обидва Vodafone, діалог доказу, анонімний 401, showcase hash та сусідній сервіс: **PASS**. Відкриття одного доказу оптимізовано за raw ID після перевірки scope.
- Образ **ufv-api:curated-20260919**, image-only overlay `/var/lib/ufv/releases/curated-20260919-compose.json`; manifest `/var/tmp/ufv-curated-release.json`. База образу — country-label; env збережено. Collectors/processor/Caddy не перезапускалися. Schema-backup root-only перед зміною. Власних commit/push не виконано. Власний тестовий API 18198 зупинено.
- Обмеження швидкості: на реальній базі контрольні цикли dashboard+feed зайняли приблизно 4–5 с для 24h/30d та 9–11 с для 7d. Окремий count місячного curated-view — 0,10 с. Це виміри конкретного зрізу, не SLA; подальша оптимізація операторських coverage-запитів залишається задачею.


Оновлено 2026-09-19. **Тестова версія розгорнута: https://hire.qpon.** Нові прямі доручення користувача дозволили код і deployment та замінили попередній етап «поки не писати код». Загальна мета хакатону з інцидентами й AI-брифом залишається наступним етапом.



## Підписи географії дашборда (2026-09-19)

За прямим уточненням користувача «Останні 24 години · Київ» замінено на **«Останні 24 години · Україна»**, аналогічно для 7/30 днів. Підписи часу в таблиці та методиці — «місцевий час». Розрахунок часових меж `Europe/Kyiv` збережено: це часовий пояс, а не географічний фільтр джерел.

Розгорнуто лише ці чотири текстові зміни поверх чинного `ufv-api:night-20260919T205351Z`, без включення інших локальних правок. Образ: `ufv-api:country-label-20260919`; overlay `/var/lib/ufv/releases/country-label-20260919-compose.json`, запис відкату `/var/tmp/ufv-country-label-release.json`. API-конфігурація збережена, інші процеси не перезапускалися. Build пройшов; перевірка в production Chromium охоплює три періоди, підписи часу та 1440/390 px.


## Нічний прогін — реліз 1 і активні виправлення (19.09, 23:59 Київ)

- Прямий дозвіл користувача: режим A, commit/push main, регулярні розгортання. `llm_allowed` Telegram не вмикається. Три потоки працюють у `night-api`, `night-ui`, `night-ai`; головне дерево й сторонні зміни разового Telegram-аналізу збережено.
- **Реліз 1 у production 20:54:59 UTC:** головна `/` — живий дашборд «Сьогодні»; стрічка `/feed`; 24 год / 7 днів / 30 днів; графіки, реакції, джерела, правила сигналів, доказові переходи з URL-фільтрами. API та processor оновлено, обидва collectors не перезапускалися. Міграції **0011 та 0013 застосовано**, не редагувати; 0012 зарезервовано AI-потоку.
- Перевірки I1: API — три ролі, ізоляція workflow, людські рішення й версії, ручний підрахунок реакцій на трьох постах, місячний шлях без доступу до текстового view; **75 tests passed** на `ufv_checks`. Typecheck/build пройдено. Початковий typecheck головного дерева потребував перебудови shared declarations; після `npm run build -w @ufv/shared` пройшов.
- I2/I3: Chromium 1440/768/390, світла/темна теми; 6 основних рендерів і 36 варіантів порожнього/помилки/завантаження/довгих рядків/вимкнених collectors/viewer, нуль перевірених переповнень і помилок JS. Знімки переглянуті як зображення. Знайдене переповнення великого числа на телефоні виправлено й повторно перевірено. Акцент dataviz пройшов валідатор обох тем.
- I4: 6 браузерних проходів «7 днів → плитка → стрічка зі зрізом → цитата/оригінал → назад»; URL зберігає часові межі. Повний демо-сценарій з усіма слотами залишається воротами наступної інтеграції.
- Після deploy: health, anonymous feed 401, справжній admin-вхід, `/`, `/feed`, `/inbox`, `/showcase`, сусідній `h1hs.com`, Chromium — PASS. Додатково 6 production-знімків без JS errors/overflow; локально `artifacts/night-production-1`, не публікувати як демо-знімки.
- **Другий погляд на живих даних відкрив дефект:** під час інвалідації цілих джерел місячні згортки часткові (за послідовні запити 7 днів — 4775 згадок, 30 днів — 1458). Фінальні ворота I1/I6 через цей дефект ще не закриті. Виправлення точності/покриття — перше активне завдання; 7-денний запит також оптимізується (5,33 с; 24 год — 3,67 с; 30 днів — 0,08 с на цьому зрізі).
- Інші відтворені дефекти: у сигналі не видно бренду, через що загальний телеком-сигнал «Увага» виглядає суперечністю до стану Vodafone; лічильник перепублікацій підписаний як кількість матеріалів; неправильне відмінювання 1/2/5; нуль конкурентних згадок залишає видиму крихітну смугу. UI-виправлення вже перевіряються, ще не розгорнуті.
- WP0: тег `pitch-base-2026-09-19` уже є на GitHub (183d1a3); main був 6383cc7. Перевірка tracked-файлів common-secret scanner дала лише синтетичний пароль тесту та посилання на змінні, підтверджених секретів немає.
- Реліз і відкат: `/var/lib/ufv/releases/night-20260919T205351Z.json`, `deploy/night-release.sh --rollback /var/lib/ufv/releases/night-20260919T205351Z.json`. Скрипт зберігає images до перемикання, перевіряє production та автоматично відкочує при невдалих післярелізних перевірках. Caddy не змінювався.
- Далі: виправлення місячних агрегатів → I5 (інтерактивні слоти, калькулятор, analyst без ключа, refresh) → повний I6 і кола доопрацювання. Усього нічного завдання **не завершено**.

## Telegram LLM-прогін завершено (2026-09-19, 20:50 UTC)

- Підстава: пряме уточнення користувача — аналізувати **весь наявний Telegram-текст і коментарі** через AGY / Gemini 3.8 Flash. Попередню внутрішню паузу в AGENTS.md для цього зрізу скасовано; RSS-прогін нижче — історія.
- **https://hire.qpon/analysis**, admin/analyst. Зріз **20:07:46 UTC**: **44 753 / 44 753** невидалені записи, зокрема **35 563 коментарі**, 9 187 дописів, 3 повідомлення груп. 21 видалений виключено; джерела з даними — 42. Run `0e1ddce1-774c-4018-9660-25258afcf62b`.
- Повний семантичний відбір: 41 888 різних текстів із контекстом, 222 пакети; до детального аналізу перейшли 94 записи (59 дописів, 34 коментарі, 1 груповий). Всі 94 результати повторно перевірені після збереження: ID та дослівні цитати item/контексту, включно з аспектами операторів. Один screening-пакет відхилено валідатором і успішно повторено.
- Поточні результати на **20:50:39 UTC**: **33 тематичні записи** — 2 Vodafone, 13 про конкурентів, 18 телеком; 29 дописів і **4 коментарі (3 negative, 1 neutral)**. Прямих тематичних коментарів про Vodafone не відібрано. 4 записи потребують перевірки, 5 рекламні, 3 spam. 1 363 результати застаріли через зміну/появу контексту та виключені; 4 записи тестових джерел поза статистикою. Ці лічильники актуальності можуть змінюватися, поки collector працює.
- AI-зведення: **5 висновків, 3 рекомендовані дії**, усі з цитатами й оригінальними Telegram URL. Для брифу використано всі 33 актуальні тематичні записи; тестові джерела виключено. Зміна будь-якого цитованого доказу приховує старий бриф. Це не верифікація повідомлених у джерелах фактів чи оцінка всіх абонентів.
- Run: 20:16:37 → 20:48:05 UTC; фінальний бриф на актуальних доказах — **20:48:44 UTC**. Повний прогін зайняв близько **32 хвилин**, а не live-латентність. Нові дані після cutoff ще не проходять цей разовий LLM-процес. Звичайний collector/processor продовжує працювати.
- **0009/0010 застосовані, не редагувати**. Progress, labels і brief окремі від `core.mentions`/людських рішень. Snapshot/checkpoints поза Git: `/var/lib/ufv/analysis-once/telegram-20260919`, 0700/0600. Тимчасові ізольовані AGY-конфіги прибрані.
- Перевірено: 21 unit-тест; `ufv_checks` (ролі, workflow, цитати, stale/deleted/новий контекст, тестові джерела, серверні фільтри, пагінація); build; production API/Chromium, світла/темна тема, 320/768/1024/1440. Пряма перевірка 19 явних брендових згадок у screening — усі передані далі; це вузький sanity check, не виміряна точність. Повний API на 44 753 результатах — 3,04 с під час контрольного запиту.
- Розгорнуто тільки API/web: `ufv-api:telegram-analysis-20260919T2035`. `/showcase/` збережений за SHA-256; Inbox/Feed/health доступні, сусідній Warblan healthy. Власний тестовий API на 18198 зупинено. Відкат — `ufv-api:before-telegram-analysis-20260919T2025`; деталі в [ANALYSIS_ONCE.md](ANALYSIS_ONCE.md).
- Наступне окремо: безперервний LLM-consumer нових даних, системна ручна оцінка якості, інциденти. Автоматичний щоденний вебпошук не підключений. Власних commit/push цей інкремент не виконував; паралельні агенти оновлювали спільне Git-дерево, їхні зміни не скидались.


## План ночі та бриф для команди (2026-09-19, 23:30 Київ) — лише документи

- Підстава: пряме доручення користувача — оновити `.gitignore` перед публікацією на GitHub, скласти точний план реалізації до пітчу з позначками місць для нейромережі та підготувати файл для команди. **Реалізація не починалась.**
- `.gitignore` розширено (ключі, `admin-bootstrap.txt`, тестові env-файли, бекапи, знімки перевірок, `TO-BE-DELETED.html`). Перевірка 140 файлів, що підуть у push, секретів не виявила — лише синтетичні значення в тестах. Commit і push виконує користувач.
- [PLAN_NIGHT.md](PLAN_NIGHT.md): шви `LLM-SEAM` S1–S7 з єдиним провайдером і роботою без ключа, брама прав `llm_allowed`, модель метрик, щільний дашборд «Сьогодні», пакети робіт із розкладом на три потоки та двома розгортаннями, перелік статичних слотів.
- [TEAM_BRIEF.md](TEAM_BRIEF.md): архітектура, стек, статус «працює / частково / слот / план», цифри із датами, перелік того, чого не стверджувати.
- Зафіксований факт: Vodafone Україна на 100% належить NEQSOL Holding, акцій на біржі немає, торгуються лише єврооблігації; ідею «акції материнської компанії» замінено слотом «єврооблігації VFU».
- Додано [NIGHT_RUN.md](NIGHT_RUN.md) — інструкцію автономного нічного прогону (цикл, ворота якості, межі, режими розгортання, ранкова передача); у плані — кінцевий образ продукту й ітерації I1–I7; у брифі — шлях одного поста, паспорт розміченого матеріалу і сценарій демо.
- Відкрито: провайдер і ключ LLM; рішення щодо `llm_allowed` для Telegram; хто виконує root-кроки розгортань. Режим розгортання вночі: A (з Docker і дозволом) чи B (команда на ранок) — типово B.

## Разова Gemini-розмітка та вебпошук (2026-09-19, після 19:53 UTC)

За прямим дорученням користувача додано **https://hire.qpon/analysis** для admin/analyst. AGY / Gemini 3.8 Flash завершив 232/232 дозволених RSS-записів зі зрізу 19:43 UTC: 230 поза темою, 2 телеком, 0 прямих Vodafone-згадок; один із двох тематичних матеріалів історичний, 2024 року. Перевірені цитати та модельні висновки збережено окремо; звичайний Feed досі працює за правилами.

На новому екрані також 5 реальних вебматеріалів, знайдених 19 вересня, і поточні Telegram-кандидати Vodafone за правилами (4 під час перевірки). Telegram у LLM **не передавався**: уточнення прав залишається без відповіді; у зрізі пропущено 29 622 записи. Автоматичного щоденного вебпошуку немає, показана разова добірка з датами.

Міграцію **0009_analysis_once.sql** застосовано; не змінювати. Оновлено лише API/web-образ, collectors/processor/Caddy не перезапускалися. 10 unit-перевірок, ізольовані ufv_checks-перевірки ролей/версій/контексту та Chromium у тестовому й production-середовищах пройшли. Докладний контракт, результати, обмеження й відкат — [ANALYSIS_ONCE.md](ANALYSIS_ONCE.md). Нижчі твердження «LLM не реалізовано» описують стан попереднього релізу; безперервного LLM-пайплайна, інцидентів та AI-брифу досі немає.

## Масове підключення Telegram-джерел (2026-09-19)

- Підстава: пряме доручення користувача приєднати наданий список і зробити джерела активними; окремо — авторизувати Telegram 2 та перенести на нього решту списку.
- Авторизовано Telegram 2 через `scripts/create_session.py --account 2`; сесія та API credentials автоматично зашифровані в БД. Обидва акаунти фактично `online`.
- Додано `scripts/import_telegram_sources.py`: root-only серверний імпорт читає protected config, приєднує лише явно передані адреси, створює/вмикає джерело і пише audit. Повторний запуск не створює дублів; короткий `FloodWait` чекає за явним `--wait-on-flood`. Collector не використовує цю можливість автономно.
- Імпортовано **66** активних публічних broadcast-каналів: 20 на Telegram 1 і 46 на Telegram 2. Нові джерела мають старт історії 7 днів, workflow 1 та ввімкнений збір. За перші секунди collector отримав 2 425 матеріалів; це live-добір, не перевірка тематичної якості або повноти.
- **15** переданих URL не активовано: `kievreal1`, `h_kyiv`, `veha_kh`, `hyevuy_dnepr`, `mzu_official` є непідтримуваними групами; `truhadnipro`, `h_odesa`, `truhazap`, `vaneek_nikolaev` не мають чинного username; `h_dnipro`, `real_dnepr`, `dnipro_info`, `real_odessa`, `lvivych`, `typical_lviv` Telegram відхилив як некоректні. Потрібні актуальні публічні канальні URL або окреме розширення collector для груп.
- Перевірено після імпорту: API/PostgreSQL/collector/processor запущені; обидва акаунти `online`; 13 нових джерел уже `watching`, решта ще проходять початковий polling. Один `error` належить старому тестовому джерелу, не новому списку.

## Production: Telegram + RSS (2026-09-19, 19:17 UTC)

Пряма вказівка користувача: перенести зміни робочих гілок у production. Інтегровано Telegram `c01f020` і RSS із `feat/rss-collector`; код перенесено в `/opt/ufv` зі збереженням паралельних змін. Міграції 0006–0008 застосовані. Редизайн основного React UI **тепер розгорнуто**; `/showcase/` збережено незмінним.

- Telegram: bulk resolve/add та явний join, публічні групи/форуми, вкладені коментарі, історія метрик, watch lifecycle/ручні overrides. У production збережено 68 джерел і 2 налаштовані акаунти; реальні нові пости, group_message та коментарі отримуються. Другий акаунт під час перевірки переходив у короткі FloodWait; це видимий cooldown, не безлімітний доступ.
- RSS: 13 записів каталогу, **8 увімкнено**, 5 очікують прав видавця. Перший цикл 230 записів / 213 різних URL / 4 accepted правилами, pending=0. Sources → RSS, Inbox і Feed доступні на домені.
- 53 інтеграційні/модульні тести, Telegram і RSS Chromium-перевірки пройшли. Авторизовані production-екрани працюють без JS/HTTP errors. API health, showcase та Warblan перевірені; акаунти, сесії й дані збережені. Резервна копія та rollback images створені.
- LLM, інциденти, AI-бриф і прогнозування залишаються **не реалізованими**. Аналіз — правила; точність не виміряна.

Деталі, джерела, обмеження й відкат: [RSS.md](RSS.md), [TELEGRAM_COMPLETE.md](TELEGRAM_COMPLETE.md). Нижче збережено попередній стан як історію.

## Редизайн інтерфейсу — історія до спільного релізу

- Підстава: пряма вказівка користувача — повний редизайн у строгому діловому стилі на зразок shadcn/ui; червону гаму не зберігати.
- Нова дизайн-система в `apps/web/src/styles` (токени, світла/темна тема, шкала шрифтів) і `components/ui.tsx`; бічна навігація, мобільне меню, перемикач теми. Шрифт Inter і значки Lucide зібрані локально, бо CSP дозволяє лише власний origin. Тексти про затримку, прогалини покриття й незалежність доказів збережено.
- Telegram-консоль перенесено з iframe (`public/console`, vanilla JS) у React-сторінки `apps/web/src/pages/telegram`. API не змінювався. Попередній frontend збережено поза Git: `var/backups/web-before-redesign-2026-09-19.tar.gz`.
- Перевірено в headless Chromium на зібраному bundle з підставним API (лише локально, у публічний сервіс синтетика не потрапляла) та продакшн-CSP: 14 екранів × 1440/768/390 px, світла й темна теми — без горизонтального переповнення, помилок CSP/консолі, розмірів поза шкалою, цілей дотику < 32 px. 20 сценаріїв форм адмінки: метод, шлях і тіло кожного запиту збігаються зі старою консоллю. `npm run typecheck` пройшов.
- **Не перевірено:** робота з живим API/БД і production. Розгортання потребує root (перебудова образу `api`, див. [DEPLOYMENT.md](DEPLOYMENT.md)); користувач `deploy` не має доступу до Docker. `apps/web/dist/assets` на хості належить root, тому локальний `npm run build` від `deploy` не може очистити `dist`; образ збирається всередині Docker і від цього не залежить.

## Showcase-макет дашборда (2026-09-19) — опубліковано

- Опубліковано за прямим запитом користувача: **https://hire.qpon/showcase/**. Працюють також `/showcase` і `/showcase/index.html`, вхід не потрібен. Це статичний демонстраційний знімок, а не live-стрічка Telegram.
- Для цього deployment поверх чинного API-образу додано лише showcase HTML і скомпільований `apps/api/src/index.ts` з окремою CSP. Основний frontend залишився на попередній версії; весь React-редизайн і Telegram-інкремент з окремої гілки не розгорталися. Collector/processor/PostgreSQL/NATS/Caddy не перезапускалися.
- Перевірено публічний HTML за SHA-256, три варіанти маршруту, вкладки та мобільний вигляд у Chromium без помилок консолі; API health — 200, анонімний feed — 401, health сусіднього сервісу — 200. Образ для відкату й деталі — [SHOWCASE_DEPLOYMENT.md](SHOWCASE_DEPLOYMENT.md).

- Підстава: пряме доручення користувача — перенести головний макет дашборда на новий дизайн-кіт одним окремим `.html` і врахувати рекомендації ментора (керівник ІТ-департаменту компанії).
- Файл: `apps/web/public/showcase/index.html` (≈190 КБ, стилі кіта, шрифт Inter, дані й скрипт вбудовано; зовнішніх запитів немає). Публічна адреса — `https://hire.qpon/showcase`. Для `/showcase` API віддає окрему CSP: inline дозволено, будь-які мережеві запити заборонено (`apps/api/src/index.ts`). Піддомен на зразок `random.hire.qpon` потребує правки спільного Caddy від root — не робилося.
- Вхідний файл користувача `/opt/ufv/TO-BE-DELETED.html` (власник root) не змінювався. У репозиторному `data.js` бракувало `SNAPSHOT_DATE` і статусів; додано лише те, що стверджує сам макет: дата знімка 19.09.2026, збій 02.07 — «вирішено», Обухів — «відкрито».
- Враховано з рекомендацій: проблема замість охоплення в критичному блоці; сильніший блок «Що зараз»; клікабельні плитки показників; пояснення метрик; фільтр за критичністю; колонка «Статус дій» із прочерками та редагуванням (у макеті — лише localStorage браузера); розділ «Вплив на Vodafone» з калькулятором; кнопка «Оновити»; вкладка «А що у конкурентів»; метрики кількості та швидкості поширення; згортання секцій; гейміфікація аналітика («Хочеш дізнатись більше?»); блок «Як згадка стає тривогою» прибрано; без рухомого рядка й блимання. Фірмовий шрифт Vodafone пропрієтарний і не вбудований: він перший у стеку й застосується, якщо встановлений на пристрої, інакше — Inter.
- Дотримання незмінних вимог: записи після «Оновити» позначені «Демо · не реальна згадка» й не входять до лічильників реальних згадок; фінансові числа — з публічної звітності (виручка 14,9 млрд грн за I пів. 2026, ARPU 154 грн, 15,1 млн клієнтів — Інтерфакс-Україна, 28.08.2026), усі похідні оцінки позначені як припущення; ставка $15/люд.-год — орієнтир ментора; кроки конкурентів мають посилання на джерела, їхня оцінка позначена як думка команди; перепублікації не названо незалежними підтвердженнями; аналітик позначений «без LLM».
- Перевірено в headless Chromium: три вкладки, оновлення, статус дій, фільтри, тултіп з клавіатури, 1440/390 px, світла й темна теми — без переповнення, помилок консолі, розмірів поза шкалою; `/showcase` працює під своєю CSP і не може звернутися до `/api`; решта шляхів зберегла основну CSP. Колір графіків перевірено валідатором палітри.
- **Не зроблено:** «статус дій» у робочій адмінці (потрібні міграція, API і права — окрема задача); системний моніторинг конкурентів; оцінка втрат для конкретного збою (у знімку немає тривалості й частки абонентів). Емодзі реакцій на цьому сервері не рендеряться через відсутність шрифту — на пристроях користувачів не перевірено.

## Аудит наступного етапу Telegram

- За запитом користувача підготовлено [TELEGRAM_NEXT.md](TELEGRAM_NEXT.md): bulk-add/join, групи, модель даних, snapshots, правила стеження 2–24 дні, релевантність, інциденти й бриф. Це пропозиція, не виконана реалізація; додаток і конфігурація під час аудиту не змінювалися.
- Стан під час перевірки 2026-09-19 близько 14:15 UTC: Telegram 1 online, джерело #1 (окрема супергрупа) має PublicBroadcastRequired; новий канал #2 — watching. Отримано 1 пост, rules-v1 відніс його до rejected: не знайдено зв'язку з телеком-тематикою. У `/inbox` він доступний; основна accepted-стрічка його не показує. Це не оцінка правильності класифікації.
- Для цього одного поста pipeline записав published_at, fetched_at і analyzed_at. Обговорень у БД поки немає; наскрізну перевірку коментарів ще не проведено. Поле raw.items.metrics наявне, але collector метрики не записує.
- Питання про 2–24 дні поставлено користувачеві: старий пост чи весь канал. Поки пропозиція плану застосовує lifecycle до поста/гілки, з продовженням збору нових постів джерела.

## Автоматичне підключення Telegram

- Виправлено `scripts/create_session.py`: після входу сесія й API-доступ автоматично шифруються та записуються у сайт, без копіювання StringSession. Є вибір місця `--account`, захист від дублів/перезапису іншої сесії та зашифроване відновлення після збою БД.
- Реальну сесію користувача з `dump-env` перенесено до Telegram 1 й перевірено збереження; plaintext прибрано. Надані користувачем API ID/hash додано зашифровано; Telegram 1 успішно підключений (`online`). Telegram 2 ще очікує авторизації.
- Міграція 0005 і admin API підтримують збережену сесію без API-доступу: запуск заборонений, у UI лише два потрібні поля.
- Оновлення розгорнуто. Chromium перевірив завершення підключення на тестовій БД, мобільну форму та production-стан першої сесії без повторного OTP. 12 тестів, збірка й health-check пройшли; live-збір ще не перевірено.

## Перше підключення до Telegram

- Користувач надав API-доступ; Telegram 1, модуль і workflow 1 увімкнені. Фактичну MTProto-авторизацію перевірено, статус `online`. Секретів у документації немає.
- Додане користувачем джерело #1 перевірено: публічна супергрупа (`megagroup=true`, `broadcast=false`), а поточний collector підтримує канали (`broadcast=true`) і їхні discussion-коментарі. На момент першого підключення джерело мало `PublicBroadcastRequired`, матеріалів було 0; актуальніший стан після додавання каналу описано вище. Окремі групи ще не підтримуються; не вважати це помилкою credentials.
- Перевірка типу виконувалася після відключення основного клієнта; після неї збір відновлено, Telegram 1 знову `online`.

## Sources, увесь вхід і два акаунти

- `/sources`: окрема вкладка, реальний перемикач Telegram, перехід до налаштувань; RSS явно недоступний.
- `/inbox`: усі зібрані матеріали всіх workflow, pending/accepted/review/rejected/deleted, фільтри, пагінація, прямий контекст. Доступ — admin/analyst; viewer/share не мають доступу.
- Міграція 0004 додає вузьке представлення `core.incoming_items`; API читає його без прямих прав на raw.items. Секретів і профілів авторів у ньому немає.
- Підготовка двох account slots ідемпотентна; незаповнений акаунт не можна ввімкнути. Поточний стан підключення описано вище.
- Перевірено API права/статуси, pending до обробки та після редагування, відсіяне й контекст, усі workflow; у Chromium — перемикач, налаштування другого акаунта, новий екран і мобільна темна тема.
- Пояснення ролей/посилань/кількох сесій: [ACCESS_AND_SOURCES.md](ACCESS_AND_SOURCES.md).

## Виконано

- Імпортовано каркас Claude зі scratchpad у `/opt/ufv`, без тимчасових `.env`/БД/паролів. Збережено Hono, Better Auth, Kysely, React, TanStack Query, shared permissions, SQL-міграції й контракт подій. Репозиторій передано `deploy:deploy`.
- Реалізовано автономний Python Telethon collector і processor, нормалізацію, contextual rules-v1, durable cursors для постів/коментарів, outbox/NATS, receipts та повторну обробку. Старий окремий FastAPI/SQLite-прототип архівовано поза Git.
- Додано admin workflow, encrypted StringSession/API hash, багато сесій, mapping каналів, керування активністю, ротацію credentials, manual review, scoped/revocable dashboard links.
- Інтегровано Telegram-адмінку в чинний React/Better Auth сайт. Початковий дизайн із GitHub замінено редизайном 2026-09-19 (див. вище); оригінальний макет не змінювався. UI показує фактичний порожній стан, правила фільтра й часові мітки; вигаданих результатів немає.
- Піднято окремий постійний PostgreSQL 18 і NATS JetStream у Docker; застосовано міграції, розділено runtime grants. API, collector, processor працюють з restart policy.
- Створено нового `admin@hire.qpon`; випадковий початковий пароль у `/etc/ufv/admin-bootstrap.txt`, root-only. Паролі тимчасової Claude-БД не підходять.
- `hire.qpon` направлено виключно на UFV через спільний Caddy. `*.hire.qpon` повертає 404. Решту доменних блоків не змінено; `https://h1hs.com/api/health` повертає ok. DNS уже вказував на сервер, зміни в Cloudflare dashboard не знадобилися.
- Командою (окремі коміти на GitHub, злиті 2026-09-19) у прототип `vodafone-monitor/` додано вкладку **AI-аналітик** зі звітами зі знімка, логотип і оновлені дані; синтаксис перевірено `node --check`. Саме ця версія прототипа стала основою showcase.
- Оновлено AGENTS, README, контекст, питання, план, архітектуру, інструкції запуску/Telegram/deployment. Commit/push не виконувалися.

## Перевірено

- `npm ci`, `npm run typecheck`, `npm run build`; збірка Docker Node/Python.
- Міграції й повторний запуск bootstrap без зміни наявних таблиць; окремі облікові ролі PostgreSQL.
- **12 тестів пройшли:** 2 перевірки правил/маскування + 10 інтеграційних сценаріїв, включно з автоматичною реєстрацією, завершенням API-доступу та відновленням після збою без нового OTP. Окрема БД `ufv_checks`, справжні Hono/Better Auth/PostgreSQL/NATS, Telegram замінено контрольованими об'єктами.
- TS encrypt → Python decrypt; секрети не повертаються; повторні raw/event/process не створюють дублікати; вкладений контекст, спам, реклама, редагування/видалення; cursor після рестарту, FloodWait, вимкнений модуль.
- 401 без сесії, 403 на admin для viewer/чужий Origin, posts-only/summary scope, заборона доступу до іншого workflow, відкликання посилань.
- NATS JetStream outbox → processor; відновлення API після завершення його PostgreSQL-з'єднань. Знайдену необроблену idle pool error виправлено, повторна перевірка пройшла.
- Chromium/Playwright локально й через production HTTPS: вхід, React-навігація, адмін-консоль, reprocess з обмеженою DB-роллю, розміри 320/768/1024/1440, темна тема, secure HttpOnly-cookie; без runtime JS/CSP errors.
- Production: сесія Better Auth пережила заміну API-контейнера; scoped link відкрився в окремому браузерному контексті, fragment прибрано, відкликання завершило доступ. Heartbeat обох worker видно в адмінці.
- Окремо на тестовій БД перевірено запис raw/outbox роллю ufv_collector та обробку/запис core роллю ufv_processor з production grants.
- Caddy config validation, HTTPS 200, API health 200, feed 401 без входу, retired hire subdomain 404, основний Warblan health 200.

Це перевірки працездатності, не оцінка точності й не повний Telegram acceptance. Реальне підключення акаунта та доставку одного поста перевірено; це ще не повний сценарій із тематичними матеріалами й коментарями.

## Не виконано / потрібні вхідні дані

- **Канал уже читається, окрема супергрупа ще не підтримується.** Потрібно завершити live acceptance на тематичних постах/коментарях та визначити розширення підтримки груп. Покриття реальних коментарів і фактичну затримку ще не перевірено.
- Права на збір/показ конкретних матеріалів і коментарів має встановити команда. Telegram-контент не передається в LLM; актуальні обмеження наведено в TELEGRAM.md.
- Реальний наскрізний сценарій хакатону, ручна вибірка якості, інциденти, AI-бриф, аналіз конкурентів, KPI та прогнозування ще не готові.
- Не підтримуються web SMS/QR-авторизація Telegram, приватні канали, OCR/аудіо, автономний auto-join/discovery collector, горизонтальні репліки collector, повне відновлення старих офлайн-редагувань/видалень.
- Немає автоматичного backup PostgreSQL або повної системи виявлення персональних даних всередині довільної прози; маскуються типові контакти, профілі авторів не збираються.
- Часткові відповіді команди зафіксовано в QUESTIONS; точний дедлайн, навички/власники задач і людина для ручної перевірки ще невідомі.

## Наступний крок

Пройти acceptance тематичного поста та обговорення на підключеному каналі, додати підтримку дозволених супергруп. Наступні інкременти запропоновано в [TELEGRAM_NEXT.md](TELEGRAM_NEXT.md); умови перевірки — [TELEGRAM.md](TELEGRAM.md). Далі — [PLAN.md](PLAN.md). Архітектурні зміни робити від [ARCHITECTURE.md](ARCHITECTURE.md), а не від старого кандидата Go або заархівованого прототипу.

Операційні команди, захищені файли й резервні копії: [DEPLOYMENT.md](DEPLOYMENT.md). Перед наступною роботою перевірити git status та застосовні AGENTS; на сервері є інші проєкти.


## Нічна інтеграція: активна паралельна правка (21:23 UTC)

Root night-run підготував `feat/night-ui` (`b020e61` + I6 fixes): WP2, WP3, WP4, WP8a, S2 UI, повні/неповні місячні агрегати. Merge у main наразі зупинено, бо паралельна сесія активно змінює `dashboard.ts`, `feed.ts`, shared контракт і готує `0020_curated_dashboard.sql`. Чужі зміни не скинуто й не перезаписано. Нові нічні міграції **0012, 0014, 0015, 0016** вже перевірені на `ufv_checks`; production поки має лише 0011/0013 із релізу 1. Потрібне узгоджене злиття curated-семантики з атомарною свіжістю агрегатів, S2 і статусом дій, після чого — один дозволений restart collectors. До злиття root виконує read-only огляд чинних екранів.
