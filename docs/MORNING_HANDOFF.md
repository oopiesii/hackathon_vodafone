# Ранкова передача · 20 вересня 2026

**Поточний пакет завершено; пауза за прямим дорученням користувача «finish current task and take a pause».** Останній реліз підтверджено 19.09 о 22:41:58 UTC / 20.09 о 01:41:58 Київ. Фаза 2 з двома чистими колами та I7 не виконані й до нового доручення не починаються. Підтверджений стан — у [STATUS](STATUS.md).

## Передача іншому інструменту: почати звідси

**Точка зупинки: завершений поточний пакет I6. Наступна робота — фаза 2, коло «Перше враження».** I1–I5 виконані для описаного поточного обсягу; реальний runtime LLM із ключем не перевірявся. Два чисті кола та I7 не завершені. Детальна матриця I1–I7/WP0–WP10 тепер стоїть на початку [PLAN_NIGHT.md](PLAN_NIGHT.md).

Під час цього оновлення змінено лише документацію за прямим проханням користувача; розробку й deployment не відновлювали. Останній код релізу — `4bb17ff`, документація попередньої передачі — `aff98c1`; обидва вже були в `origin/main`. Поточний коміт передачі дивитися через `git log -1`.

1. Прочитати `AGENTS.md`, верхній запис `STATUS.md`, нову матрицю в `PLAN_NIGHT.md`, цей файл, потім процедуру `NIGHT_RUN.md` і сценарій `TEAM_BRIEF.md`.
2. Перевірити `git status`, `git log`, `git worktree list` та віддалені зміни. Канонічне дерево — `/opt/ufv`, гілка `main`. Старі нічні worktree вже інтегровані за змістом; не зливати їх повторно навмання. `/home/deploy/ufv-context` — окремий паралельний потік, його не скидати. Чужий untracked `docs/Screenshot_20260919_230340.png` збережений, у наші коміти не входить.
3. Звірити поточний production з контрольним релізом нижче. Для перевірки без зміни конфігурації є `scripts/check_live_dashboard.py` і `scripts/check_night_browser.py` (виконують власний вхід). Не запускати fixtures у production; `ufv_checks` на момент передачі вільна, тестові API й агенти завершили роботу.
4. Після доручення продовжувати — почати нове повне коло фази 2. Записати до 7 відтворюваних дефектів, виправляти за пріоритетом, перевіряти знімки й сценарій демо. Попередній огляд із виправленнями не зараховувати за два чисті кола. Не починати із повторної реалізації I1–I6.
5. Після приймання поточного UI можна брати I7: завершити окрему сторінку конкурентів (WP5), UI статусів дій поверх чинного API (WP7), потім WP8b/WP9 за пріоритетами плану. WP6 уже реалізований. Оптимізація операторського підрахунку 7d лишається окремим боргом; не повторювати повільніший кандидат з [DASHBOARD_QUERY_REVIEW](DASHBOARD_QUERY_REVIEW.md).

Приклади наявних перевірок для наступного виконавця; під час цієї документаційної передачі повторно не запускалися:

```bash
cd /opt/ufv
.venv/bin/python scripts/check_live_dashboard.py
UFV_SCREENSHOTS=artifacts/handoff-next-review .venv/bin/python scripts/check_night_browser.py
```

Скрипти очікують локальний захищений конфіг і браузер; секрети не копіювати в інший інструмент або повідомлення. Для точкового browser-прогону є `UFV_PAGE=sources,rss,inbox,telegram-operations`. `networkidle` сам по собі не гарантує готовність даних: перед оцінкою RSS та інших асинхронних сторінок дочекатися потрібного контенту. Інтеграційні suites серіалізувати: частина fixtures скидає спільну `ufv_checks`.

Приватні артефакти цієї машини, не Git: `artifacts/night-full-release-20260919` (повний огляд), `artifacts/night-i6-weekly` (графік і підписи), `artifacts/night-i6-flow` (переходи), `artifacts/night-i6-live` та `artifacts/night-i6-postdeploy` (останній production). Знімки з реальними текстами не публікувати як демо. У публічному брифі вже є окремі синтетичні ілюстрації.

Межі передачі: нових ключів немає, Telegram LLM вимкнений; 8 RSS дозволені для AI, 5 заблокованих джерел не вмикати. Collector restart цієї ночі використано; для наступних UI/API-виправлень обирати лише потрібні сервіси. Міграції 0011–0016, 0020–0022, 0030–0031 уже застосовані разом із попередніми; нові номери спершу звіряти з фактичним каталогом і БД. Чинні права, semantic gate, видимий pending і атомарну повноту місяця зберігати.

## Що вже видно на сайті

На [hire.qpon](https://hire.qpon) працюють тематичний dashboard/feed із завершеною разовою Gemini-розміткою, явний перехід до Vodafone за 7 днів, окремі негативні/іронічні/сумні реакції та межі покриття. API/UI-реліз 21:53 UTC додав ізоляцію кешу між сеансами й прив'язку спільного доступу до конкретного посилання. Відкрита стара вкладка потребує перезавантаження.

Нічний пакет **розгорнуто о 01:19 Київ**: позачерговий збір зі станами черги, демо мережі, сценарний калькулятор, окремий analyst без ключа, S1/S2 та фізичні місячні агрегати. Analyst має свіжий heartbeat і `waiting_key`. Контрольні прийняті матеріали: 24h — **0**, 7d — **33**, 30d — **34**, Vodafone за 7 днів — **2**; переходи до джерел і доказів збігаються. Після повного огляду виправлено тижневий графік, повільні запити, підписи зведення й операторських лічильників та переклад діагностики Telegram. API/UI й analyst оновлено окремо о 01:41 Київ; collectors повторно не перезапускалися.

## Ілюстрації інтерфейсу

Усі знімки в цьому документі зроблено на **синтетичних даних у локальному браузері**. Числа, назви проблем і реакції ілюструють вигляд; вони не є результатами вимірювання production. Персональні дані й реальні тексти повідомлень у знімки не включено.

![Дашборд на синтетичних даних, світла тема](img/night-dashboard-synthetic-light.png)

[Темна тема](img/night-dashboard-synthetic-dark.png) · [Демо мережі на телефоні](img/night-network-demo-mobile.png) · [Сценарний калькулятор на телефоні](img/night-impact-scenario-mobile.png).

## Перевірки та відкат

- 136 backend-тестів пройдено на окремій `ufv_checks`; після злиття — 6 цільових S2/query-тестів і curated acceptance. Typecheck/build, 2 тести групування тижня, 9 unit-тестів зведення пройдено. Реальних Telegram/LLM викликів у цих тестах немає.
- Перевірено ролі, human/context invalidation, атомарний місяць, legacy RSS scope, refresh/backoff, analyst mocks і нові/наявні grants.
- Синтетичний Chromium: 1440/768/390, дві теми; переходи до доказів, калькулятор і демо мережі. Окремий acceptance із реальною тестовою БД — 320/390/768/1440, дві теми.
- Повний production-огляд: 80/80 комбінацій сторінки/ширини/теми, усі 156 PNG розглянуто. Після виправлень перевірено графік, зведення, Sources/RSS/inbox та Operations у двох темах; 6 сценаріїв «графік → стрічка → доказ → назад» пройдено локально.
- Після останнього релізу: **4/4** live-переходи «денний стовпчик → стрічка → доказ → назад» та **16/16** комбінацій Sources/RSS/inbox/Operations на 1440/390 у двох темах пройдено. Усі 8 знімків графіка/зведення та 16 основних знімків сторінок розглянуто; реальні тексти лишаються приватними артефактами поза Git.
- Контрольний live-зріз після останнього релізу: 24h — **0**, 7d — **33**, 30d — **34**, Vodafone 7d — **2**. Джерела, конкуренти, документи та всі 8 денних стовпчиків узгоджені з feed. API: день **1,071с**, тиждень **4,760с**, місяць **0,413с**; це окремі вимірювання, не SLA. Analyst має свіжий heartbeat, `waiting_key`; Telegram LLM — 0 джерел.
- Єдиний ручний production-refresh: Telegram pickup **1,883с**, RSS **2,107с**. RSS завершив 8 джерел; Telegram оновив 41 і відклав решту за чинними лімітами/чергою. Ліміти не обходилися.

Поточні images: **`ufv-api:night-20260919T224055Z`**, **`ufv-analyst:night-20260919T224055Z`**; pipeline/collectors — `ufv-pipeline:night-20260919T221752Z`. Останній manifest має `verified`; код `4bb17ff`. Нових міграцій у пакеті виправлень немає. Єдиний нічний restart collectors використано попереднім повним релізом.

```bash
deploy/night-release.sh --rollback /var/lib/ufv/releases/night-20260919T224055Z.json
```

Команда повертає **лише API й analyst** до перевіреного повного релізу 22:17; processor і collectors залишаються поточними. Адитивна схема зберігається. Відкат не виконувався. Manifests 21:53/22:15 після 0030 не застосовувати: вони повертають API без потрібної перевірки повноти місяця.

Залишковий борг: операторський підрахунок за 7 днів потребує подальшого прискорення (SQL близько 3,8 с); повільніший кандидат відхилено. Це запис для відновлення роботи, не активне завдання під час паузи. Сервіси продовжують працювати; агентські потоки й тестовий API зупинені, `ufv_checks` вільна.

## Межі для презентації

Ключ постійного LLM не додавався. Telegram `llm_allowed` не вмикався. Завершений разовий аналіз і майбутній безперервний consumer — різні режими. Прогалини після cutoff залишаються видимими; настрої відібраних коментарів не представляють усіх абонентів.

Мережа — позначене демо; Threads/X не підключені; калькулятор показує припущення, а не збитки. Автоматичні інциденти, прогнозування кризи, перевірена точність і чат на живих даних не заявляються готовими. Картка конкурентів веде до прийнятих матеріалів; окрему сторінку конкурентів поки не створено.

## Порядок показу журі

1. **Тематичний відбір і докази:** «Сьогодні» → Vodafone за 7 днів → матеріал → цитата й оригінал. Нуль за добу пояснюється часом публікації, а не доведеною відсутністю проблем.
2. **Реакції й покриття:** частка негативних emoji, окремі іронія/сум, час вимірювань і pending-вхід. Пороги уваги — налаштування демо.
3. **Слоти й сценарій впливу:** демо регіон → вишка та калькулятор сценарію з джерелом і формулою.

Краще не відкривати першим **«Увесь вхід»**: це операторський масив до відбору, він містить сторонні теми. Для першого враження використовувати тематичну стрічку.

Контракти й відповіді для команди: [TEAM_BRIEF](TEAM_BRIEF.md), [CURATED_DASHBOARD](CURATED_DASHBOARD.md), [ANALYST](ANALYST.md), [NIGHT_I6_REVIEW](NIGHT_I6_REVIEW.md).
